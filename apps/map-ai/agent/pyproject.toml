[tool.coverage.run]
branch = true
source = [ "map_ai_agent" ]

[tool.coverage.report]
exclude_lines = ['if TYPE_CHECKING:']
show_missing = true

[tool.pytest.ini_options]
addopts = """
 --cov
 --cov-report html:'../../../coverage/apps/agent/map-ai-agent/html'
 --cov-report xml:'../../../coverage/apps/agent/map-ai-agent/coverage.xml'
 --html='../../../reports/apps/agent/map-ai-agent/unittests/html/index.html'
 --junitxml='../../../reports/apps/agent/map-ai-agent/unittests/junit.xml'
"""

[project]
name = "map-ai-agent"
version = "1.0.0"
description = "Automatically generated by Nx."
requires-python = ">=3.9,<4"
readme = 'README.md'
dependencies = ["fastapi", "uvicorn", "langgraph", "langchain-openai", "langchain-core", "pydantic"]

[tool.hatch.build.targets.wheel]
packages = ["map_ai_agent"]

[tool.hatch.metadata]
allow-direct-references = true

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.ruff]
exclude = [
  ".ruff_cache",
  ".svn",
  ".tox",
  ".venv",
  "dist",
]

line-length = 88
indent-width = 4

[tool.ruff.lint]
select = [
  # pycodestyle
  "E",
  # Pyflakes
  "F",
  # pyupgrade
  "UP",
  # flake8-bugbear
  "B",
  # flake8-simplify
  "SIM",
  # isort
  "I",
]
ignore = []

fixable = ["ALL"]
unfixable = []
