{
  "name": "map-ai",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/map-ai/web/src",
  "projectType": "application",
  "tags": ["type:app", "domain:project-xjw", "area:map-portal"],
  "// targets": "to see all targets run: nx show project map-ai --web",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "continuous": true,
      "options": {
        "command": "vite --host 127.0.0.1 --port 5173",
        "cwd": "apps/map-ai/web"
      }
    }
  }
}
