# 内网测试环境 - 前端 Web 配置模板
# 用途：内网测试环境部署

# 环境标识
VITE_DEPLOY_ENV=test
VITE_NODE_ENV=production

# 天地图 Token 改由 API 下发(同 dev，不再注入 bundle)

# 浏览器运行时 API 基础地址
# 部署到 Nginx 时保持为空字符串，走同源 /api 代理
VITE_API_BASE_URL=

# 浏览器运行时 IAM 基础地址
# 部署到 Nginx 时保持为空字符串，走同源 /auth、/system 代理
VITE_IAM_BASE_URL=
